function [output_idxs] = Find_Gradient_Changes(input_dets)
   % takes the input determinants from the 
   
   % upper limit
   N_max = length(input_dets);
    
   % absolute value of the determinant
   abs_det = abs(input_dets);
   
%    % loop over every index
%    for idx = 1:N_max-1
%        
%        abs_det(idx) > abs_det(idx + 1)
%       
%        
%        
%    end
   
    
end